---
const targetDate = new Date('2025-06-20T00:00:00Z'); // Fecha objetivo

// Función para calcular el tiempo restante
function calculateTimeLeft() {
  const now = new Date();
  const difference = targetDate.getTime() - now.getTime(); // Aseguramos que sea un número

  if (difference <= 0) {
    return { days: 0, hours: 0, minutes: 0, seconds: 0 };
  }

  const days = Math.floor(difference / (1000 * 60 * 60 * 24));
  const hours = Math.floor((difference % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
  const minutes = Math.floor((difference % (1000 * 60 * 60)) / (1000 * 60));
  const seconds = Math.floor((difference % (1000 * 60)) / 1000);

  return { days, hours, minutes, seconds };
}

let timeLeft = calculateTimeLeft();
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cuenta Regresiva</title>
  </head>
  <body>
    <div class="countdown">
      <span id="days">{String(timeLeft.days).padStart(3, '0')}</span>
      <span class="colon">:</span>
      <span id="hours">{String(timeLeft.hours).padStart(2, '0')}</span>
      <span class="colon">:</span>
      <span id="minutes">{String(timeLeft.minutes).padStart(2, '0')}</span>
      <span class="colon">:</span>
      <span id="seconds">{String(timeLeft.seconds).padStart(2, '0')}</span>
    </div>
    
    <script type="module" is:inline>
      const targetDate = new Date('2025-06-20T00:00:00Z');

      function calculateTimeLeft() {
        const now = new Date();
        const difference = targetDate.getTime() - now.getTime();

        if (difference <= 0) {
          return { days: 0, hours: 0, minutes: 0, seconds: 0 };
        }

        const days = Math.floor(difference / (1000 * 60 * 60 * 24));
        const hours = Math.floor((difference % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((difference % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((difference % (1000 * 60)) / 1000);

        return { days, hours, minutes, seconds };
      }

      function updateCountdown() {
        const timeLeft = calculateTimeLeft();
        document.getElementById('days').textContent = String(timeLeft.days).padStart(3, '0');
        document.getElementById('hours').textContent = String(timeLeft.hours).padStart(2, '0');
        document.getElementById('minutes').textContent = String(timeLeft.minutes).padStart(2, '0');
        document.getElementById('seconds').textContent = String(timeLeft.seconds).padStart(2, '0');
      }

      setInterval(updateCountdown, 1000);
    </script>
  </body>
</html>

<style>
  .countdown {
    margin: 5%;
    font-family: Arial, sans-serif;
    font-size: 3rem;
    text-align: center;
    padding: 10px;
    border-radius: 8px;
    margin-top: 20px;
    display: inline-flex;
    align-items: center;
  }

  .countdown span {
    margin: 0;
    font-weight: bold;
    color: white;
    text-shadow: 0 0 10px white;
  }

  .countdown .colon {
    margin: 0 3px;
    font-weight: bold;
    color: white;
    display: inline-block;
    
  }

  .countdown span:first-child {
    margin-right: 5px;
  }

  .countdown span:not(:last-child) {
    margin-right: 3px;
  }
</style>
